import { AccessLevelPipe } from './access-level.pipe';

describe('AccessLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new AccessLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
